# Strategic merge patch for staging: lower resource footprint.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: acme-gateway
spec:
  template:
    spec:
      containers:
        - name: acme-gateway
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              memory: 128Mi
